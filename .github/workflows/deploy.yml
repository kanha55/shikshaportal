name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Deploy placeholder
        run: |
          echo "Production deploy to Oracle Cloud will be configured in Sprint 3."
          echo "Required GitHub secrets: DEPLOY_KEY, SERVER_IP, SERVER_USER"

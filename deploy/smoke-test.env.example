# Copy to deploy/smoke-test.env (gitignored) for production smoke credentials.
# Never commit real passwords.

SMOKE_BASE_URL=https://greenvalley.dskl.in
SMOKE_ADMIN_EMAIL=principal@greenvalley.test
SMOKE_ADMIN_PASSWORD=changeme
SMOKE_STUDENT_EMAIL=rahul@greenvalley.test
SMOKE_STUDENT_PASSWORD=changeme

# Optional: run 50 parallel public API requests after main checks
# SMOKE_LOAD_TEST=1
# SMOKE_LOAD_COUNT=50

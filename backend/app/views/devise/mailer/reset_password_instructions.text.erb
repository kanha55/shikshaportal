Hello <%= @resource.name || @resource.email %>,

Someone requested a password reset for your Shiksha Portal account.

Reset token: <%= @token %>

Use this token in the app to set a new password. The link expires in 6 hours.

If you did not request this, ignore this email.

— Shiksha Portal
